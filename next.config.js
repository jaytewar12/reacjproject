/**  @type {import('next').NextConfig} 
const nextConfig = {
  reactStrictMode: true,
};

exports default nextConfig;/*

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig; 